So, from creating the CLI & TUI for Mac OS, I understand that linux machines also need the similar app. Insted of people going into config files, and changing the mode to not shutting down when lid is closed. And then going and reenabling it. We will make the special tui for cli for them. 

Now I need the clarification if its actually gonna be working on Linux.


So because Linux doesn't have the menu bar system we will just make an app or we will we can make some kind of shortcut that will open the pop up with set time. Or a shortcut to activate the preset the app will be identically the same as with on boarding dashboards everything is the same we should redo every command so it is designed for Lennox for the install option I'm thinking about the Pac-Man or maybe other ones to install or just normal Linux file for the app. 
How did we should discuss our system how the commands will change or actually wait I'm dumb they will not change only the app code will change so we should discuss that


And for our website I'm thinking about if our website will detect that our that website viewer is on Lennox it will change the website theme to be for Lennox doesn't matter about the Coffeetosh name. The thing I want to change is I have the 3-D model of a laptop which will be used for Lennox and for Windows it will replace the existing Mac the phones will not be replaced. 